<?php
declare(strict_types=1);

namespace ##Application##\##Name##\Application\##Name##\Command\Reinstate;

use ##Application##\##Name##\Domain\VO\ID;

class Reinstate##Name##CommandHandler
{
    public function __construct(
        private \##Application##\##Name##\Domain\Repository\##Name##RepositoryInterface $repository
    ) {
    }

    public function handle(Reinstate##Name##Command $command): void
    {
        $id = ID::fromString($command->id);
        $##Name## = $this->repository->findById($id);

        if ($##Name## === null) {
            throw new \RuntimeException('##Name## not found');
        }

        $##Name##->reinstate();

        $this->repository->save($##Name##);
    }
}
