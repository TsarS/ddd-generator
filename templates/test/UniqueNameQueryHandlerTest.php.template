<?php
declare(strict_types=1);

namespace ##AppName\Tests\Application\Query\Unique;

use PHPUnit\Framework\TestCase;
use ##AppName\##Application##\Application\##Name##\Query\Unique\Unique##Name##Query;
use ##AppName\##Application##\Application\##Name##\Query\Unique\Unique##Name##QueryHandler;
use ##AppName\##Application##\Domain\Entity\##Name##;
use ##AppName\##Application##\Domain\Repository\##Name##RepositoryInterface;
use ##AppName\##Application##\Domain\VO\ID;
use Mockery;

class Unique##Name##QueryHandlerTest extends TestCase
{
    private Unique##Name##QueryHandler $handler;
    private $repository;

    protected function setUp(): void
    {
        $this->repository = Mockery::mock(##Name##RepositoryInterface::class);
        $this->handler = new Unique##Name##QueryHandler($this->repository);
    }

    public function testHandleChecksUniqueness(): void
    {
        $query = new Unique##Name##Query('Unique Name');

        $this->repository
            ->shouldReceive('isUnique')
            ->with('Unique Name')
            ->andReturn(true);

        $result = $this->handler->handle($query);

        $this->assertTrue($result);
    }
}